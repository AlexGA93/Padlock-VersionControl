// import action types

//define an initial state
const initialState = {};


const service = (state=initialState, action) => {
    switch(action.type){
        default:
            return state;
    }
}

export default service;